# Modèle de configuration frontend (Vite).
# Copier en .env.local pour surcharger en local (non versionné).
# Les variables exposées au navigateur DOIVENT commencer par VITE_.

# URL de base du backend FastAPI. Par défaut : http://localhost:8000
VITE_API_BASE_URL=http://localhost:8000
