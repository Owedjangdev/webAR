# Lu automatiquement par Vite en mode production (npm run build).
# Le front buildé appelle ce backend (Render). En dev (npm run dev), c'est .env
# qui s'applique (proxy Vite), pas ce fichier.
VITE_API_BASE_URL=https://webar-3h71.onrender.com
